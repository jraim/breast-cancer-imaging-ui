version: "3.7"

services:
  web:
    container_name: react_app
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3100:3000"
    volumes:
      - /app/node_modules
      - .:/app